# ============================================================
# Vertical Pod Autoscaler (VPA) — Booking Platform
# Cluster: dev-eks-cluster | Namespace: default
# ============================================================
# VPA recommends right-sized CPU/memory requests based on
# actual usage. Mode: "Off" = recommendations only (safe).
# Change to "Auto" to let VPA update requests automatically.
#
# IMPORTANT: Do NOT use VPA Auto mode together with HPA CPU/memory.
# Use VPA for right-sizing recommendations, HPA for scaling.
# ============================================================

# ── Book Service (Java/Spring Boot) ───────────────────────────
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: book-vpa
  namespace: default
  labels:
    app: book
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: book
  updatePolicy:
    updateMode: "Off"           # "Off" = recommendations only
  resourcePolicy:
    containerPolicies:
      - containerName: book
        minAllowed:
          cpu: 100m
          memory: 256Mi
        maxAllowed:
          cpu: 2000m
          memory: 4Gi
        controlledResources: ["cpu", "memory"]

---
# ── User Service (Node.js) ─────────────────────────────────────
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: user-vpa
  namespace: default
  labels:
    app: user
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: user
  updatePolicy:
    updateMode: "Off"
  resourcePolicy:
    containerPolicies:
      - containerName: user
        minAllowed:
          cpu: 50m
          memory: 128Mi
        maxAllowed:
          cpu: 1000m
          memory: 2Gi
        controlledResources: ["cpu", "memory"]

---
# ── Search Service (Node.js) ───────────────────────────────────
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: search-vpa
  namespace: default
  labels:
    app: search
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: search
  updatePolicy:
    updateMode: "Off"
  resourcePolicy:
    containerPolicies:
      - containerName: search
        minAllowed:
          cpu: 50m
          memory: 128Mi
        maxAllowed:
          cpu: 1000m
          memory: 2Gi
        controlledResources: ["cpu", "memory"]

---
# ── Frontend Service (Node.js) ─────────────────────────────────
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: frontend-vpa
  namespace: default
  labels:
    app: frontend
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: frontend
  updatePolicy:
    updateMode: "Off"
  resourcePolicy:
    containerPolicies:
      - containerName: frontend
        minAllowed:
          cpu: 50m
          memory: 128Mi
        maxAllowed:
          cpu: 1000m
          memory: 2Gi
        controlledResources: ["cpu", "memory"]